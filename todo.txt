- Clear session cookie when user deletes self (perhaps unnecessary, since next request with invalid session cookie will
  receive response that clears cookie)

- Potential state corruption due to concurrency

- Periodically purge expired sessions by calling deleteByLessThanOrEqualToExpiration()

- Configure Spring to eliminate null JSON fields when writing HTTP response bodies

- SameSite cookies

- Replace nested fetch() statements with chained fetch() statements
  How to terminate the chain prematurely?

- Handle native browser navigation (back button, rewriting URLs, etc)

- Figure out URL encoding (uppercase, whitespace, etc)

- Restrict allowed characters for API endpoints