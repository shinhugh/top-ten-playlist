package org.dev.toptenplaylist.service;

import org.springframework.stereotype.Service;

@Service
public class SecureHashManager implements SecureHashService {
    @Override
    public String hash(String text) {
        // TODO
        return text;
    }
}
